---
---

Upgrade Stripe SDK from v20 to v21. No code changes required — codebase uses integer cent amounts (unit_amount, amount) rather than decimal_string fields affected by the Decimal type breaking change.
