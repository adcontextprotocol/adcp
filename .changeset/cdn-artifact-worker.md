---
---

Add the Cloudflare Worker that serves R2-backed protocol artifacts with the same `/schemas/v*` and `/compliance/v*` alias resolution as the current Fly middleware. No package release is needed.
