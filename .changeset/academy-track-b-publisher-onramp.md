---
---

Add "Is this track for you?" non-module section at the top of `docs/learning/tracks/publisher.mdx`: sell-side value prop, role → module mapping table (ad ops, sales, engineer, executive), and B4 build-goal preview. No new module number; the triage content lives as a landing-page section so the X0 pattern doesn't need to be created across Track C (buyer) and Track D (platform). Refs #2572.
