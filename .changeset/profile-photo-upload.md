---
"adcontextprotocol": patch
---

Add profile photo upload to the community profile edit page.

Members can now upload JPEG/PNG photos (max 2MB) directly instead of pasting an external avatar URL. Includes magic byte validation, rate limiting, and security headers.
