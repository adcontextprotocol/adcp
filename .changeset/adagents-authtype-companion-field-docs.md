---
"adcontextprotocol": patch
---

Clarify `authorization_type` companion field names in `adagents.json` schema descriptions and documentation. Adds a six-row quick-reference table to the Authorization Patterns section, adds an explicit warning that `inline_properties` uses a companion field named `properties` (not `inline_properties`), and updates the schema `description` for the `inline_properties` branch to name this exception.
