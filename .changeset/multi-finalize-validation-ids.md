---
"adcontextprotocol": patch
---

Add stable validation IDs and tighter atomic-success assertions to the refine finalize-exclusivity storyboard so multi-finalize grading failures identify the exact failing assertion in reports.
