---
---

Empty changeset: server and documentation behavior only. Adds approved AAO-hosted brand logo asset URLs for `brand.json`, keeps unapproved uploads private, records provenance for promoted logo rows, and documents the public `/assets/brands/{domain}/{assetId}.{ext}` URL shape.
