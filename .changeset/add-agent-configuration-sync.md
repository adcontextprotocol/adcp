---
"adcontextprotocol": minor
---

Add the experimental `sync_agent_configuration` task for stable-authenticated-principal connection settings, reusable reporting destinations, capability-change webhook compatibility, optimistic concurrency, and credential-free destination setup state.
