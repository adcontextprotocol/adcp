---
"adcontextprotocol": minor
---

Add the experimental `sync_agent_configuration` task for delegated-user or autonomous-workload connection settings, reusable reporting destinations, capability-change webhook compatibility, optimistic concurrency, principal isolation, and credential-free destination setup state. Interactive clients such as Claude do not have to register as buyer agents, but must propagate a stable delegated user identity.
