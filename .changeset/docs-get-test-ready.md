---
---

docs: add "Get Test-Ready" operator guide bridging build-an-agent and validate-your-agent. Covers the three surfaces the storyboard runner needs — `get_adcp_capabilities`, sandbox bootstrap via `sync_accounts`/`list_accounts`, and `comply_test_controller` — and shows the `createComplyController` seller-side scaffold from `@adcp/client/testing@5.8.0`. Includes a readiness checklist and explicit guidance against teaching-to-the-test by hardcoding fixture IDs. Updates `schemas-and-sdks` to list the new seller-side and server exports.
