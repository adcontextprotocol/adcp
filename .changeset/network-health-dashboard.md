---
---

Network health dashboard and alerting for managed publisher networks. Adds admin UI at /admin/network-health, authenticated API endpoints at /api/network-health/*, Slack webhook notifications, and configurable alert thresholds.
