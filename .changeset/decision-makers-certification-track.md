---
---

Add the AdCP for Decision-Makers certification track — a standalone, free, reasoning-only credential for brand leaders, agency executives, and SMB owners who evaluate, brief, and decide on AI-native advertising but do not build agents. Migration 513 seeds track L (badge_type NULL, so it does not satisfy the Practitioner specialization-track requirement), three free interactive modules (L1: the reversed data flow; L2: data/brand/governance as the control surface; L3: deciding and leading), the `adcp_decision_maker` badge, and the `decision_makers` tier-1 credential (requires only L1–L3, no prerequisite credential). Every success criterion carries a stable semantic id for recertification, and no criterion requires running code or querying a live agent — assessment is strategic reasoning verified by Sage through conversation. Adds docs pages under `docs/learning/decision-makers/` that route the previously orphaned monetizing-ai, adcp-vs-openrtb, and FAQ measurement narratives into the curriculum, plus discovery entries in the certification overview and docs.json navigation.
