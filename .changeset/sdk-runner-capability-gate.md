---
---

Bump `@adcp/sdk` to `9.0.0-beta.31` so hosted storyboard runs use the upstream
absent `requires_capability.equals` gate fix, and add regression coverage for
skipping storyboards when the gated capability path is omitted.
