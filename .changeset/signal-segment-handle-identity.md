---
"adcontextprotocol": patch
---

Clarify that `signal_agent_segment_id` is an opaque, provider-scoped resolved segment handle that buyers should echo verbatim when present, especially for package signal targeting and future signal-driven creative fan-out.
