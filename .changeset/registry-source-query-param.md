---
---

`/api/registry/agents` accepts an optional `?source=registered|discovered` query parameter to filter the response by registration source. Default behavior (no param) is unchanged — both registered and discovered agents are returned merged.
