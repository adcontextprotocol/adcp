---
---

Adds `specs/java-sdk-rfc.md` proposing a first-class Java SDK to reach L0–L3 parity with `@adcp/sdk` (TS, GA) and `adcp` (Python, beta). Captures JVM-specific architecture decisions (Java 17 baseline, sync + virtual threads, Jackson + JDK HttpClient, framework-neutral core + Spring Boot starter), surface mapping to 5 Maven artifacts, codified spec gotchas, design-partner gating, and the funding question for the Builders Working Group.
