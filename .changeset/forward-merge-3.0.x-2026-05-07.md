---
---

chore(forward-merge): manually resolve 3.0.x → main backlog (closes #4189)

Reconciles 3 stuck forward-merge runs (since 2026-05-03). Per-file resolution policy documented on PR #4191. Empty changeset — merge-only commit, no protocol surface change. The 3.0.x content being propagated has its own changesets on the patch line.
