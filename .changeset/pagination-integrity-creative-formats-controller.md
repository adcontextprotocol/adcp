---
"adcontextprotocol": patch
---

Mark the `pagination_integrity_creative_formats` storyboard as controller-gated so agents that intentionally omit `comply_test_controller` skip the storyboard at load time instead of cascading into mid-storyboard fixture seeding skips.
