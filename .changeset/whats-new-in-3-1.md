---
"adcontextprotocol": patch
---

New `docs/reference/whats-new-in-3-1.mdx` — announcement-quality 3.0 → 3.1 page targeting adopters reading the beta release. Synthesizes the headlines from `release-notes.mdx § Version 3.1.0` into a friendlier entry point: at-a-glance comparison table, one-paragraph summaries of the eight major features (distributed brand.json, dependency-impact webhooks, release-precision version negotiation, webhook foundation + buyer-side delivery visibility, windowed pull recovery, vendor-metric optimization goals, reach_window / viewed_seconds delivery reporting, canonical creative formats), an adopter-action-by-role table, and migration links.

Distinct from `whats-new-in-v3.mdx` (which stays as the v2 → v3 major-version landing). The 3.1 page links back to the v3 page for major-version context and forward to `release-notes.mdx` for per-PR detail.

Added to `docs.json` nav under both existing **AdCP 3.0** groups (the group's scope effectively spans 3.x migration content). A 3.1 announcement nav promo can follow separately.
