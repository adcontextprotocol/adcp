---
---

feat(certification): add the S7 Brand Protocol specialist module + `specialist_brand` credential (migration 514), the missing `verify_brand_claim` / `verify_brand_claims` sandbox fixture on the training agent's brand tenant (payload-envelope JWS `signed_response` under a new response-signing key), `authorized_operators[]` on the served `brand.json` + Sportshaus walkthrough fixture, the `S7: Brand` docs page + nav, and two global Sage teaching-quality fixes (no meta-narration; affirmation on stacked corrections) plus a capstone brevity guardrail. Certification + training-agent + docs only — implements the existing brand-protocol spec, changes no schema; no package release.
