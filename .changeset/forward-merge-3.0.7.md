---
---

ci(release): forward-merge 3.0.x → main (3.0.7 backlog).

Lands the 3.0.7 release artifacts (`dist/protocol/3.0.7.tgz*`, `dist/schemas/3.0.7/`, `dist/compliance/3.0.7/`, `dist/docs/3.0.7/`) on main so adcontextprotocol.org can serve the versioned URLs, plus the 3.0.x patch backlog (#4013, #4024, #4092, #4184, #4192, #4193, #4218). No new release-notes entry — patches already shipped via 3.0.x.
