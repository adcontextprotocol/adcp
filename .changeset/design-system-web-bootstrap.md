---
---

Bootstrap `apps/web/` — React + Vite + TanStack Router + shadcn/ui scaffold for the design-system migration. Includes Ladle stories for all primitives, a tiered color token system (semantic + brand-blue ramp + system status colors with `--destructive`/`--success`/`--warning`/`--info` pairs), and the initial component library (Button, Badge, Card, Dialog, Form, Input, Select, Switch, Table, Tooltip, etc.). Migration plan updated with token-convention exceptions to revisit.
