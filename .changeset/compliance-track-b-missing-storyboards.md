---
"adcontextprotocol": patch
---

Add storyboard coverage for three previously-uncovered tasks: `search_brands` (brand protocol), `get_creative_features` and `get_media_buy_artifacts` (content-standards specialism). These are additive phases on existing storyboards; no existing steps or validation paths are changed.

Partial progress on issue #4291 Track B. Remaining uncovered tasks (`tasks_get`, `tasks_list`, `creative_approval`, `update_rights`) require design decisions documented in the triage comment on that issue.
