---
---

`apps/web/`: replace Tailwind's `blue-*` ramp with Scope3 brand-blue values (anchored at `blue-600` = #3055F8); add `-1000` stops to all Tailwind primitive ramps for deep flood backgrounds. `--primary` now points at `blue-600` in both light and dark mode. `--info` and `--chart-3` now collapse onto the brand blue (intentional — to be repointed in a follow-up).
