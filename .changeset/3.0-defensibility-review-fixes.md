---
---

Apply pre-ship defensibility review fixes ahead of 3.0 GA.

**From first review pass (landed in the main merge at 8052b4478):**

- Remove stale "AdCP 3.0 Proposal — under development" banners from four Property Governance pages; property governance ships in 3.0 as the `property-lists` specialism.
- Replace `scope3_agent` / `scope3_list` variable names in property-governance code examples with vendor-neutral `suitability_agent` / `suitability_list`. Same fix removes "Scope3, Claude" from the signals orchestrator example.
- Downgrade unqualified "privacy-preserving collaboration" on the signals ecosystem data-warehouse card to "clean-room collaboration". Matches the audit already done for `hashed_email`/`hashed_phone` language in privacy-considerations.
- `SECURITY.md` supported-versions table now lists 3.x as current, 2.5 as security-only until 2026-08-01, with pointer to the v2 sunset page.
- `CONTRIBUTING.md` adds the fictional-brands-only editorial rule for examples, test vectors, and storyboards, pointing at `CLAUDE.md` and `static/compliance/source/universal/fictional-entities.yaml`.
- `docs/faq.mdx` adds two entries: Prebid's ownership of the sell-side reference implementation (pointing to the AdExchanger handoff piece), and "Do AdCP examples use real brand names?" surfacing the fictional-entities editorial rule.
- `docs/governance/overview.mdx` adds a line noting the policy registry ships seeded with US (FHA, ECOA, EEOC, COPPA, FDA, FTC, TTB), EU (DSA, prescription DTC), and platform-specific policies alongside GDPR Article 9 restricted-attribute definitions.
- `README.md` Governance section names the Prebid handoff, notes Apache 2.0's express patent grant, and points at the fictional-brand rule in CONTRIBUTING.

**From second review pass (this commit):**

- `docs/reference/known-limitations.mdx:47` tense fix — #2310 shipped 2026-04-18, so schema enforcement of `authority_level: agent_full` rejection on `fair_housing`/`fair_lending`/`fair_employment` is present-tense. Previously read "3.0 GA will reject … before GA, enforcement depends on the governance-agent implementation" — stale.
- `docs/reference/release-notes.mdx:16` trust-framing — reword from "cryptographically verifiable and retry-safe" to "retry-safe and auditable, with optional end-to-end request signing for AdCP Verified agents" so the hero sentence doesn't overclaim. RFC 9421 request signing is optional in 3.0 and required only for AdCP Verified.
- `docs/reference/whats-new-in-v3.mdx` storyboard/compliance section — add a `<Note>` explicitly stating AdCP Verified is self-attested in 3.0: agents publish passing `runner-output.json`, AAO does not audit or gate issuance, third-party audit is 3.1.
- `docs/governance/policy-registry.mdx:282` CSBS provenance — qualify the "contributed to AAO by Scope3" line to note formalization of the IP donation instrument is tracked in #2314 (moved to Evergreen — requires legal sign-off). CSBS ships as seeded policy under AAO custodianship until the signed assignment is on file.

Launch-day cutover checklist (FAQ rc.4 → GA flip, intro banner removal, package-version bump, docs-site version-selector default) tracked separately in #2538.
