---
---

Add new `docs/reference/versions.mdx` enumerating every published AdCP version with status (Active / Security-only / End of life / Planned), wire-pin value, and EOL dates. Replaces a recurring class of Addie questions ("what version is current, what's deprecated, where's 4.0") with a single fast-scan page. Cross-linked from `release-notes`, `changelog`, `versioning`, and `choose-your-sdk`; added to nav in both the `3.0` and `latest` blocks. Closes #4427.

Docs-only change — no protocol schema changes.
