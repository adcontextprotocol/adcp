---
---

Add recommendation for using MCP transport session ID as fallback for context_id in agent implementations. Documents how agents can use the MCP SDK's `extra.sessionId` when clients don't provide explicit `context_id`, enabling automatic session persistence for simple clients like ChatGPT and Claude.
