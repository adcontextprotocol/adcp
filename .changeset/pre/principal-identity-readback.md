---
"adcontextprotocol": patch
---

Let side-effect-free `get_principal` return a distinct `recognized` result for an existing durable principal identity before standing configuration exists, without read-time materialization.
