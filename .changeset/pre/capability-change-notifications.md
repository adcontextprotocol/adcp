---
"adcontextprotocol": minor
---

Add agent-level `capabilities.changed` notifications for cached `get_adcp_capabilities` responses, plus account-anchored `account.status_changed` notifications for durable account lifecycle changes. Includes freshness metadata, registration schemas, webhook payload documentation, and read-side account webhook activity.
