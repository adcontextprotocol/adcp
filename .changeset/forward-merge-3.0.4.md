---
---

Forward-merge 3.0.x → main bringing v3.0.4's release artifacts (dist/{compliance,schemas,protocol}/3.0.4/) and content. No version bump on main. The underlying spec changes were already documented and released as part of v3.0.4 with their own changesets on 3.0.x; this merge just reconciles the lines.
