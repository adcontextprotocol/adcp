---
"adcontextprotocol": minor
---

Add seller-optimized shared budgets across media-buy packages. Media buys and proposals can now delegate cross-package allocation to the seller under an aggregate budget, optimization goals, pacing, package caps, and soft minimum-spend targets, while preserving fixed package budgets as the default.
