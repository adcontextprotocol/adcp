---
---

Extends Sage's "NEVER reference content you haven't shown" teaching rule to cover multi-turn tool result references. When Sage discusses a specific item from a prior turn's tool result, it must now re-state the relevant field values in plain language in the current message rather than assuming the learner can scroll back to a large payload returned earlier in the conversation.
