---
"adcontextprotocol": minor
---

Define the verified canonical `brand.json` Agent URL as the stable identity input for RFC 9421 signed callers, preserving principal continuity across key rotation while keeping distinct Agent URLs isolated.
