---
---

Bump dev dependencies via dependabot npm_and_yarn group: `mintlify` 4.2.525 → 4.2.531 and transitive `body-parser` 1.20.1 → 1.20.5 inside `@mintlify/previewing`. Both changes are dev-only (mintlify is in `devDependencies`, and `body-parser` is not a direct dependency of the server runtime — production uses Express's bundled bodyParser). No runtime behavior change.
