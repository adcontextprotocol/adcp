---
---

docs(canonical-formats): clarify product format declarations vs format option references

Empty changeset: this PR only clarifies documentation/schema descriptions and adds a regression test for the existing schema boundary. It does not change the published protocol wire shape or require a package version bump.
