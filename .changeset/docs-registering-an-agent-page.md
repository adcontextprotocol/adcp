---
---

New `docs/registry/registering-an-agent.mdx` page documenting the four crawl paths into the registry, the registered-vs-discovered trust distinction, and what AAO membership unlocks at the API surface (members_only and private agent visibility on /api/registry/operator). Closes the docs gap for non-members who today have no first-class self-registration path. Linked from the Registry API nav. Refs #3538.
