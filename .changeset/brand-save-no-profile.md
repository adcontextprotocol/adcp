---
---

Allow saving brand identity (logo, color) before a member profile exists. The brand-identity endpoint now falls back to org data when no profile is found, deriving brand domain from the logo URL hostname.
