{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "$id": "/schemas/core/duration.json",
  "title": "Duration",
  "description": "A time duration expressed as a value and unit. Used for frequency cap windows, reach optimization windows, and other time-based settings that support sub-day granularity.",
  "type": "object",
  "properties": {
    "value": {
      "type": "integer",
      "minimum": 1,
      "description": "Number of time units"
    },
    "unit": {
      "type": "string",
      "enum": ["hours", "days"],
      "description": "Time unit"
    }
  },
  "required": ["value", "unit"],
  "additionalProperties": true
}
