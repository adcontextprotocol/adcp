{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "$id": "/schemas/core/creative-preview.json",
  "title": "Creative Preview",
  "description": "A sample creative preview generated by the seller for a product. Represents how the seller would render an ad using the buyer's catalog and brand assets. These are representative samples — the full set of creatives is generated at execution time when the buyer commits via create_media_buy.",
  "type": "object",
  "properties": {
    "generated_creative_ref": {
      "type": "string",
      "description": "Unique identifier for this preview within the product. Buyers reference this in refinement feedback to give per-creative direction (e.g., 'gen_abc needs shorter headlines')."
    },
    "format_id": {
      "$ref": "/schemas/core/format-id.json",
      "description": "Format this preview was generated for. References a format from the product's format_ids."
    },
    "renders": {
      "type": "array",
      "description": "Rendered preview outputs. One entry per rendered piece (primary, companion, etc.). Sellers SHOULD use url output_format to avoid inflating buyer agent context windows with inline HTML.",
      "items": {
        "$ref": "/schemas/creative/preview-render.json"
      },
      "minItems": 1
    },
    "catalog_item_ref": {
      "type": "string",
      "description": "Identifier of the catalog item this preview was generated from. References an item_id, GTIN, or other identifier from the buyer's catalog. Absent when the preview is not catalog-driven."
    },
    "description": {
      "type": "string",
      "description": "What this preview shows. Helps buyers evaluate the creative approach without rendering the preview."
    },
    "ext": {
      "$ref": "/schemas/core/ext.json"
    }
  },
  "required": ["generated_creative_ref", "format_id", "renders", "description"],
  "additionalProperties": true
}
