{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "$id": "/schemas/core/dimensions.json",
  "title": "Dimensions",
  "description": "Pixel dimensions for visual formats (width and height must be specified together). This schema is designed for composition via allOf - consuming schemas should define their own additionalProperties constraints.",
  "type": "object",
  "properties": {
    "width": {
      "type": "integer",
      "minimum": 1,
      "description": "Width in pixels"
    },
    "height": {
      "type": "integer",
      "minimum": 1,
      "description": "Height in pixels"
    }
  },
  "required": ["width", "height"]
}
