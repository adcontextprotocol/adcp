{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "$id": "/schemas/core/reporting-adjustment-receipt.json",
  "title": "Reporting Adjustment Receipt",
  "x-status": "experimental",
  "description": "Authenticated consumer acceptance or rejection of one exact post-official adjustment. It does not alter the original official revision or its receipt.",
  "type": "object",
  "properties": {
    "reporting_receipt_id": { "type": "string", "minLength": 16, "maxLength": 255, "pattern": "^[A-Za-z0-9_.:-]{16,255}$", "x-entity": "reporting_receipt" },
    "reporting_adjustment_id": { "type": "string", "minLength": 1, "maxLength": 255, "pattern": "^[A-Za-z0-9_.:-]{1,255}$", "x-entity": "reporting_adjustment" },
    "adjusts_reporting_revision_id": { "type": "string", "minLength": 1, "maxLength": 255, "pattern": "^[A-Za-z0-9_.:-]{1,255}$", "x-entity": "reporting_revision" },
    "status": { "type": "string", "enum": ["accepted", "rejected"] },
    "observed_adjustment_sha256": { "type": "string", "pattern": "^[A-Fa-f0-9]{64}$", "description": "Digest recomputed from the adjustment using its canonical evidence rule." },
    "rejection_codes": { "type": "array", "items": { "type": "string", "minLength": 1, "maxLength": 128, "pattern": "^[A-Z][A-Z0-9_]*$" }, "minItems": 1, "uniqueItems": true },
    "observed_at": { "type": "string", "format": "date-time" },
    "received_at": { "type": "string", "format": "date-time", "readOnly": true }
  },
  "required": ["reporting_receipt_id", "reporting_adjustment_id", "adjusts_reporting_revision_id", "status", "observed_adjustment_sha256", "observed_at"],
  "allOf": [
    {
      "if": { "properties": { "status": { "const": "rejected" } }, "required": ["status"] },
      "then": { "required": ["rejection_codes"] },
      "else": { "not": { "required": ["rejection_codes"] } }
    }
  ],
  "x-adcp-validation": {
    "authorization": "Derive consumer identity from authenticated transport. The adjustment and its official revision must be visible to that caller/account; unavailable and unauthorized identifiers are indistinguishable.",
    "acceptance_match": "accepted requires observed_adjustment_sha256 to equal the referenced adjustment's canonical_adjustment_sha256 and adjusts_reporting_revision_id to equal its target. A digest or semantic disagreement is rejected with stable rejection_codes.",
    "immutability": "A reporting_receipt_id is immutable. Exact retries are idempotent; reuse with different content is a conflict."
  },
  "additionalProperties": false
}
