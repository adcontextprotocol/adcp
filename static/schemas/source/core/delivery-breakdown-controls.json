{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "$id": "/schemas/core/delivery-breakdown-controls.json",
  "title": "Delivery Breakdown Controls",
  "description": "Common limit and ordering controls for an opt-in delivery-reporting dimension.",
  "type": "object",
  "properties": {
    "limit": {
      "type": "integer",
      "minimum": 1,
      "default": 25,
      "description": "Maximum number of rows to return. Defaults to 25."
    },
    "sort_by": {
      "$ref": "/schemas/enums/sort-metric.json",
      "description": "Metric used to order rows. Falls back to spend when unavailable at this row grain; on fallback sort_direction resets to desc.",
      "default": "spend"
    },
    "sort_direction": {
      "$ref": "/schemas/enums/sort-direction.json",
      "description": "Direction for sort_by ordering.",
      "default": "desc"
    }
  },
  "additionalProperties": true
}
