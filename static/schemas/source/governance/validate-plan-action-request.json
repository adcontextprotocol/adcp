{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "$id": "/schemas/governance/validate-plan-action-request.json",
  "title": "Validate Plan Action Request",
  "description": "Validate an orchestrator action against a campaign plan before it is sent to a seller.",
  "type": "object",
  "properties": {
    "plan_id": {
      "type": "string",
      "description": "The plan to validate against."
    },
    "operator": {
      "type": "string",
      "format": "uri",
      "description": "URL of the agent making the request."
    },
    "buyer_campaign_ref": {
      "type": "string",
      "description": "Buyer's campaign identifier. The governance agent tracks state per campaign within a plan."
    },
    "tool": {
      "type": "string",
      "description": "The AdCP tool being validated (e.g., 'create_media_buy', 'get_products')."
    },
    "payload": {
      "type": "object",
      "description": "The full tool arguments, exactly as they would be sent to the seller.",
      "additionalProperties": true
    }
  },
  "required": ["plan_id", "operator", "buyer_campaign_ref", "tool", "payload"],
  "additionalProperties": false
}
