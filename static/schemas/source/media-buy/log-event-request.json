{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "$id": "/schemas/media-buy/log-event-request.json",
  "title": "Log Event Request",
  "description": "Request parameters for logging conversion or marketing events",
  "type": "object",
  "properties": {
    "measurement_id": {
      "type": "string",
      "description": "Measurement ID configured on the account via sync_measurement_events"
    },
    "test_event_code": {
      "type": "string",
      "description": "Test event code for validation without affecting production data. Events with this code appear in the platform's test events UI."
    },
    "events": {
      "type": "array",
      "description": "Events to log",
      "items": {
        "$ref": "/schemas/core/conversion-event.json"
      },
      "minItems": 1,
      "maxItems": 10000
    },
    "context": {
      "$ref": "/schemas/core/context.json"
    },
    "ext": {
      "$ref": "/schemas/core/ext.json"
    }
  },
  "required": ["measurement_id", "events"],
  "additionalProperties": true
}
