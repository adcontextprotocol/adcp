{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "$id": "/schemas/portfolio/delete-portfolio-request.json",
  "title": "Delete Portfolio Request",
  "description": "Request parameters for archiving or permanently deleting a portfolio",
  "type": "object",
  "properties": {
    "portfolio_id": {
      "type": "string",
      "description": "Unique identifier for the portfolio to delete"
    },
    "mode": {
      "type": "string",
      "enum": ["archive", "permanent"],
      "default": "archive",
      "description": "'archive' marks as archived but preserves data, 'permanent' deletes all data"
    },
    "context": {
      "$ref": "/schemas/core/context.json"
    },
    "ext": {
      "$ref": "/schemas/core/ext.json"
    }
  },
  "required": ["portfolio_id"],
  "additionalProperties": true
}
