{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "$id": "/schemas/pricing-options/cpa-option.json",
  "title": "CPA Pricing Option",
  "description": "Cost Per Acquisition pricing. Advertiser pays when a specified conversion event occurs. The event_type field declares which event triggers billing (e.g., purchase, lead, app_install). If fixed_price is present, it's fixed pricing. If absent, it's auction-based.",
  "type": "object",
  "properties": {
    "pricing_option_id": {
      "type": "string",
      "description": "Unique identifier for this pricing option within the product"
    },
    "pricing_model": {
      "type": "string",
      "const": "cpa",
      "description": "Cost per acquisition (conversion event)"
    },
    "event_type": {
      "$ref": "/schemas/enums/event-type.json",
      "description": "The conversion event type that triggers billing (e.g., purchase, lead, app_install)"
    },
    "currency": {
      "type": "string",
      "description": "ISO 4217 currency code",
      "pattern": "^[A-Z]{3}$",
      "examples": ["USD", "EUR", "GBP", "JPY"]
    },
    "fixed_price": {
      "type": "number",
      "description": "Fixed price per acquisition. If present, this is fixed pricing. If absent, auction-based.",
      "minimum": 0
    },
    "floor_price": {
      "type": "number",
      "description": "Minimum acceptable bid for auction pricing (mutually exclusive with fixed_price). Bids below this value will be rejected.",
      "minimum": 0
    },
    "price_guidance": {
      "description": "Optional pricing guidance for auction-based bidding",
      "$ref": "/schemas/pricing-options/price-guidance.json"
    },
    "min_spend_per_package": {
      "type": "number",
      "description": "Minimum spend requirement per package using this pricing option, in the specified currency",
      "minimum": 0
    }
  },
  "required": ["pricing_option_id", "pricing_model", "event_type", "currency"],
  "additionalProperties": true
}
