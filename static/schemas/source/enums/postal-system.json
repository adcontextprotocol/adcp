{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "$id": "/schemas/enums/postal-system.json",
  "title": "Postal Code System",
  "description": "Country-local postal code system names for geographic targeting. Use with an explicit ISO 3166-1 alpha-2 country. `postal_code` is the normal postal code string for countries without a more specific registered local system.",
  "type": "string",
  "enum": [
    "postal_code",
    "zip",
    "zip_plus_four",
    "outward",
    "full",
    "fsa",
    "plz",
    "code_postal",
    "postcode",
    "cep",
    "pin",
    "custom"
  ]
}
