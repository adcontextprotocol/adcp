{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "$id": "/schemas/enums/catalog-type.json",
  "title": "Catalog Type",
  "description": "The type of catalog being referenced. Determines the item schema and how the platform resolves catalog items.",
  "type": "string",
  "enum": [
    "offerings",
    "product"
  ]
}
