{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "$id": "/schemas/v1/media-buy/list-authorized-properties-response.json",
  "title": "List Authorized Properties Response",
  "description": "Response payload for list_authorized_properties task",
  "type": "object",
  "properties": {
    "adcp_version": {
      "type": "string",
      "description": "AdCP schema version used for this response",
      "pattern": "^\\d+\\.\\d+\\.\\d+$"
    },
    "properties": {
      "type": "array",
      "description": "Array of all properties this agent is authorized to represent",
      "items": {
        "$ref": "/schemas/v1/core/property.json"
      }
    },
    "tags": {
      "type": "object",
      "description": "Metadata for each tag referenced by properties",
      "additionalProperties": {
        "type": "object",
        "properties": {
          "name": {
            "type": "string",
            "description": "Human-readable name for this tag"
          },
          "description": {
            "type": "string",
            "description": "Description of what this tag represents"
          }
        },
        "required": ["name", "description"],
        "additionalProperties": false
      }
    },
    "summary": {
      "type": "object",
      "description": "High-level summary of the property portfolio to help buying agents quickly determine relevance before examining full property details",
      "properties": {
        "primary_channels": {
          "type": "array",
          "description": "Primary advertising channels represented in this property portfolio",
          "items": {
            "type": "string",
            "enum": ["display", "video", "audio", "native", "dooh", "ctv", "podcast", "retail", "social"]
          },
          "minItems": 1
        },
        "primary_markets": {
          "type": "array",
          "description": "Primary geographic markets (ISO 3166-1 alpha-2 country codes) where properties are concentrated",
          "items": {
            "type": "string",
            "pattern": "^[A-Z]{2}$"
          },
          "minItems": 1
        },
        "total_properties": {
          "type": "integer",
          "description": "Total number of properties in this portfolio",
          "minimum": 0
        },
        "description": {
          "type": "string",
          "description": "Markdown-formatted description of the property portfolio, including inventory types, audience characteristics, and special features",
          "minLength": 1,
          "maxLength": 5000
        }
      },
      "additionalProperties": false
    },
    "errors": {
      "type": "array",
      "description": "Task-specific errors and warnings (e.g., property availability issues)",
      "items": {
        "$ref": "/schemas/v1/core/error.json"
      }
    }
  },
  "required": ["adcp_version", "properties"],
  "additionalProperties": false
}