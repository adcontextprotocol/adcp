[project]
name = "adcp-docs"
version = "0.1.0"
description = "AdCP Documentation Dependencies"
requires-python = ">=3.11"
dependencies = [
    "adcp>=1.4.0",
]

[tool.hatch.build.targets.wheel]
packages = []
