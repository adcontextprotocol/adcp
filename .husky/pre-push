#!/bin/sh

# Verify version sync before pushing
npm run verify-version-sync

# Skip Docusaurus build - production uses Mintlify
# Docusaurus build fails on Mintlify-specific syntax (e.g., <Tab> components)
# If you need to validate Docusaurus build, run: npm run build
# npm run build
